# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'geo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
pod 'Alamofire', '~> 4.0’
pod 'GoogleMaps'

  # Pods for geo

  target 'geoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'geoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
